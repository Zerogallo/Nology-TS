# Nology-TS

# Cashback App - Desafio Nology

## Como executar localmente

### Backend:
```bash
cd backend
pip install -r requirements.txt
python app.py


## **Para testar localmente:**

```bash
# Clone ou crie a pasta
mkdir Nology-ts
cd Nology-ts

# Crie os arquivos na estrutura acima

# Instale dependências
cd backend
pip install -r requirements.txt

# Configure banco (ou use SQLite para teste rápido)
# Altere DATABASE_URL no app.py para usar SQLite se preferir

# Rode o backend
python app.py

# Abra o frontend/index.html no navegador